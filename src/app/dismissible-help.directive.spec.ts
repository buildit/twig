import { DismissibleHelpDirective } from './dismissible-help.directive';

describe('DismissibleHelpDirective', () => {
  it('should create an instance', () => {
    const directive = new DismissibleHelpDirective();
    expect(directive).toBeTruthy();
  });
});
