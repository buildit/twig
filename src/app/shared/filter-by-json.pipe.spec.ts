import { FilterByJsonPipe } from './filter-by-json.pipe';

describe('FilterByJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
