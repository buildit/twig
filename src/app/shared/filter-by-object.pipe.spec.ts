import { FilterByObjectPipe } from './filter-by-object.pipe';

describe('FilterByObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
