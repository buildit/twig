platform: linux

image_resource:
  type: docker-image
  source:
    repository: benaychh/random
    tag: latest

inputs:
  - name: twig-with-dist

params:
  DOCKER_USERNAME
  DOCKER_PASSWORD

run:
  path: twig-with-dist/concourse/scripts/deploy_docker.sh
